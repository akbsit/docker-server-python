ARG PYTHON_VERSION=3.9
FROM python:${PYTHON_VERSION}

ARG WORKDIR
WORKDIR ${WORKDIR}

ARG REQUIREMENTS_FILE
COPY ./${REQUIREMENTS_FILE} ${WORKDIR}
RUN pip3 install --upgrade pip -r ${REQUIREMENTS_FILE}

COPY .${WORKDIR} .${WORKDIR}

ARG EXPOSE=5000
EXPOSE ${EXPOSE}
